import 'package:flutter/material.dart';

Widget appBar(BuildContext context) {
  return AppBar(
    // elevation: 0.0,
    title: Text(
      'Chat App',
      style: TextStyle(fontSize: 22.0),
    ),
  );
}
